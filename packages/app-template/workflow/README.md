# __APP_NAME__

<!--
This README is the App Store listing body for __APP_NAME__. It is shown to
people deciding whether to install, alongside metadata from
.rome_store/rome_store.yaml.

Write it as product copy, not as technical documentation:

- Lead with what the workflow does for the user, in one or two sentences.
- List the main inputs, outputs, and benefits as short bullets.
- Mention when someone would run this workflow.
- Skip the implementation: no schemas, file paths, build commands, or API names.
- Keep it short. A few screens of scrolling is too long.
-->

One-line tagline: what __APP_NAME__ helps someone run.

## What it does

A short paragraph (2-4 sentences) describing the workflow in plain language.
Focus on the result the user gets, not how it is built.

## Outputs

- **Output one** - what the run produces, and why it matters.
- **Output two** - another concrete result or decision the workflow returns.
- **Output three** - keep these tight; cut anything that is not a headline.

## When to run it

A sentence or two on the moments where this workflow earns its place on the
guardian's dashboard. Examples of real situations work better than abstractions.
