---
"rome-pantheon": minor
---

Add dashboard-managed `ROME_TOKEN` credentials for Pantheon store API access. Tokens can be generated, reset, and revoked from settings, and store API routes now accept `rome_` bearer tokens alongside session JWTs.
