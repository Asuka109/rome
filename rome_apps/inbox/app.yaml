formatVersion: 1
id: inbox
name: Inbox
icon: assets/icon.svg
version: 0.2.13
description: Incoming-message triage, routing, and channel hooks
appRoot: dist

web:
  manifest: web/manifest.json
  displayName: Inbox
  navLabel: Inbox

agents:
  - agents/sentinel.yaml

actions:
  - actions/message-handler

hooks:
  - hooks/channel-message
